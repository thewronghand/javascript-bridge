const KEY = {
  up: 'U',
  down: 'D',
  retry: 'R',
  quit: 'Q',
};

const SIGN = {
  opener: '[',
  closer: ']',
  divider: '|',
  success: ' O ',
  fail: ' X ',
  empty: '   ',
};

const STATUS = {
  success: '성공',
  fail: '실패',
  next: '속행',
};

module.exports = { KEY, SIGN, STATUS };
